namespace Api.Controllers.Airlines.SearchParameters
{
    internal class ByActive
    {
        //internal static AirlineResult Get(
        //    string searchParameter,
        //    string search,
        //    List<Airline> airlines
        //)
        //{
        //    var isActive = search.ToLower() switch
        //    {
        //        "yes" or "y" or "true" or "ye" => true,
        //        "no" or "n" or "false" or "nah" => false,
        //        _ => throw new Exception()
        //    };

        //    return new AirlineResult()
        //    {
        //        Parameter = searchParameter,
        //        Airlines = airlines.Where(x => x.IsActive == isActive).ToList()
        //    };
        //}
    }
}
